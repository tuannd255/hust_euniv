<% if @support.master_class_subject.errors.any? %>
  $("#class_subject_form").html("<%= j render '/admin/master_class_subjects/form', support: @support %>");
<% else %>
  $('#master-class-subject-table').html('<%= j render "master_class_subject_table",\
    search: @support.search, master_class_subjects: @support.master_class_subjects %>');
  // $("#flash").html("<%= j render 'layouts/flash' %>");
  <% if(params[:create_and_continue]) %>
    // dung js de load lai form
  <% else %>
    // $(".modal").modal('hide');
  <% end %>
<% end %>
