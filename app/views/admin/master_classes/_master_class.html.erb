<tr>
  <th><%= master_class.code %></th>
  <th><%= master_class.name %></th>
  <th><%= link_to t("buttons.edit"), edit_admin_master_class_path(master_class) %></th>
  <th><%= link_to t("buttons.delete"), admin_master_class_path(master_class),
    method: :delete, data: {confirm: t("confirm")} %></th>
</tr>
